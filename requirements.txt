# --- core (always needed) -------------------------------------------------
numpy>=1.24
opencv-python>=4.8
pyyaml>=6.0
rich>=13.0
typer>=0.12

# --- detection + tracking (YOLO26) ----------------------------------------
ultralytics>=8.4.0
torch>=2.2
torchvision>=0.17

# --- CPU-fast ONNX plate detection + global OCR ---------------------------
onnxruntime>=1.18
fast-plate-ocr>=1.0
open-image-models>=0.3

# --- Persian (Farsi) OCR + RTL text rendering -----------------------------
hezar>=0.40
arabic-reshaper>=3.0
python-bidi>=0.4
pillow>=10.0

# --- analytics / dashboard ------------------------------------------------
pandas>=2.0
streamlit>=1.30
plotly>=5.19
